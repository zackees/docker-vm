{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The viewer has no filesystem, shell, process, dialog, or network plugin permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
