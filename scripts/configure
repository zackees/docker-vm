#!/usr/bin/env bash
set -euo pipefail
cat <<'EOF'
No persistent desktop configuration is created.

The private viewer creates a fresh tmpfs-backed session at launch. Run:
  git submodule update --init --recursive
  ./scripts/preflight --strict
  cd viewer/src-tauri && cargo tauri build
EOF
